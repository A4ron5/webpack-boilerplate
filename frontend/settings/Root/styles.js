import styled from 'styled-components';
import { getColor } from 'common/styles';

export default styled.div`
	width: 100%;
	height: 100%;
	font-family: 'SF';
	background-color: ${getColor('white')};
`;
